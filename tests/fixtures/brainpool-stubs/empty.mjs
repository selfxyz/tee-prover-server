// Test stub for src/verifier/primitives/brainpool.rs: exits cleanly (status
// 0) without writing anything to stdout, simulating an empty response. Must
// be caught as `EcdsaError::Structural`, not `Failed`.
process.exit(0);
