apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "tee-prover-server.serviceAccountName" . }}
  labels:
    {{- include "tee-prover-server.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
